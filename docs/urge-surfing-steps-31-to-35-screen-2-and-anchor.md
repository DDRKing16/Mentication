# Urge Surfing — Steps 31–35

## Scope completed

31. Added a keyboard- and screen-reader-usable location list as a genuine alternative to the body map. It contains every source-defined region and closes after selection.
32. Made body location and sensation controls real, exclusive selections. A selected map hotspot, list row, and sensation control all reflect the same session data.
33. Guarded body-to-anchor continuation in both the interface and session reducer. It requires one location (or future environment cue) and one sensation; Back keeps the entered body data intact.
34. Captured and compared the rendered Find the Pull screen against the supplied source screen. The implemented production screen uses the approved atomic body-map assets and interaction controls rather than presenting the supplied full-screen reference as a non-interactive image.
35. Built the first functional Set Your Anchor screen: local anchor text, bounded 90–180 second Choice Window controls, and a disabled start action until an anchor is entered. The explanation avoids a fixed biological claim and makes the outcome a deliberate pause rather than an expectation that the urge disappears.

## Live checks

- Choose from a list exposes all ten body locations; choosing Chest updates the map selection.
- Selecting TIGHT enables Continue only after a body location is selected.
- Anchor Back returns to Find the Pull with Chest and TIGHT still selected.
- Anchor text enables Start my window; the duration control changes from 90 to 120 seconds.

## Explicit visual boundary

The supplied full-screen boards remain the visual authority. Their detailed rendered figure is not used as a static imitation in the app; the body screen is constructed from the delivered, source-approved atomic body-map artwork and semantic controls so that it remains interactive and accessible. Further screen-specific visual refinement is reserved for the later visual-correction stages rather than replacing this functional UI with a screenshot.
