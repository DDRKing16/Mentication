diff --git a/src/components/PMRV2Stage.jsx b/src/components/PMRV2Stage.jsx
index 9f87e22..d84cc3a 100644
--- a/src/components/PMRV2Stage.jsx
+++ b/src/components/PMRV2Stage.jsx
@@ -42,6 +42,27 @@ function findPMRReleaseCue(tokens = []) {
   return null;
 }
 
+function findPMRCue(tokens = [], cueWords = []) {
+  const cues = new Set(cueWords);
+
+  for (const token of tokens) {
+    if (token?.wordIndex == null) continue;
+    if (cues.has(normalisePMRWord(token.text))) return token.wordIndex;
+  }
+
+  return null;
+}
+
+const TENSION_CUES = {
+  hands: ["tense", "squeeze", "clench"],
+  shoulders: ["lift", "raise"],
+  face: ["tense", "tighten", "clench"],
+  torso: ["tighten", "brace", "tense"],
+  hips: ["tense", "squeeze"],
+  thighs: ["tense", "tighten"],
+  lowerLegs: ["tense", "press"],
+};
+
 const BODY_REGIONS = [
   "hands",
   "shoulders",
@@ -128,6 +149,12 @@ const REGION_MARKERS = {
   whole: [{ top: "50%", left: "50%" }],
 };
 
+const FACE_FOCUS_MARKERS = {
+  jaw: [{ top: "15.5%", left: "50%" }],
+  eyes: [{ top: "11.8%", left: "50%" }],
+  forehead: [{ top: "8.4%", left: "50%" }],
+};
+
 const PHASE_COPY = {
   intro: "SETTLE IN",
   tense: "TENSE",
@@ -174,8 +201,8 @@ function GlowBoxes({ name, active, phase, settled }) {
   ));
 }
 
-function MarkerPoints({ region, phase, stepIndex, reducedMotion }) {
-  const points = REGION_MARKERS[region] || [];
+function MarkerPoints({ region, phase, stepIndex, reducedMotion, points: suppliedPoints }) {
+  const points = suppliedPoints || REGION_MARKERS[region] || [];
 
   return points.map((point, index) => (
     <div
@@ -213,6 +240,7 @@ function BodyFigure({
   released,
   reducedMotion,
   stepIndex,
+  markerPoints,
 }) {
   const activeRegion = region === "whole" ? null : region;
   const settled = new Set(released);
@@ -236,6 +264,9 @@ function BodyFigure({
     region !== "whole" &&
     !reducedMotion;
 
+  const glowPhase =
+    phase === "release" || movementPhase === "release" ? "release" : "tense";
+
   const maskAsset = BODY_ASSET;
 
   return (
@@ -264,18 +295,20 @@ function BodyFigure({
           draggable={false}
         />
 
-        {Object.entries(PMR_POSE_ASSETS).map(([poseRegion, src]) => {
+        {Object.entries(PMR_POSE_ASSETS).flatMap(([poseRegion, src]) => {
           const isCurrent = poseRegion === activeRegion;
+          const sides = poseRegion === "hands" ? ["left", "right"] : [null];
 
-          return (
+          return sides.map((side) => (
             <img
-              key={poseRegion}
+              key={side ? `${poseRegion}-${side}` : poseRegion}
               src={src}
               alt=""
               className={[
                 "pmr-v2-body-img",
                 "pmr-v2-motion-pose",
                 `pmr-v2-motion-pose--${poseRegion}`,
+                side ? `pmr-v2-motion-pose--${poseRegion}-${side}` : "",
                 isCurrent ? "is-current" : "",
                 isCurrent && isTensing ? "is-tense" : "",
                 isCurrent && isReleasing ? "is-release" : "",
@@ -284,7 +317,7 @@ function BodyFigure({
                 .join(" ")}
               draggable={false}
             />
-          );
+          ));
         })}
 
         <div
@@ -301,7 +334,7 @@ function BodyFigure({
               key={name}
               name={name}
               active={activeRegion}
-              phase={movementPhase}
+              phase={glowPhase}
               settled={settled.has(name)}
             />
           ))}
@@ -329,6 +362,7 @@ function BodyFigure({
             phase={movementPhase}
             stepIndex={stepIndex}
             reducedMotion={reducedMotion}
+            points={markerPoints}
           />
         )}
       </div>
@@ -393,21 +427,88 @@ export default function PMRV2Stage({
     [tokens]
   );
 
+  const tensionCueWordIndex = useMemo(
+    () => findPMRCue(tokens, TENSION_CUES[region] || []),
+    [tokens, region]
+  );
+
   const narrationHasReachedRelease =
     narrate &&
     releaseCueWordIndex != null &&
     visibleCount > releaseCueWordIndex;
 
+  const narrationHasReachedTension =
+    narrate &&
+    tensionCueWordIndex != null &&
+    visibleCount > tensionCueWordIndex;
+
+  const fallbackTensionReached =
+    phase === "tense" &&
+    (!narrate || tensionCueWordIndex == null) &&
+    phaseProgress >= 0.12;
+
   const fallbackReleaseReached =
     !narrate &&
     phase === "tense" &&
     phaseProgress >= 0.72;
 
-  const movementPhase =
-    phase === "tense" &&
-    (narrationHasReachedRelease || fallbackReleaseReached)
-      ? "release"
-      : phase;
+  let movementPhase = phase;
+
+  if (phase === "tense") {
+    if (narrationHasReachedRelease || fallbackReleaseReached) {
+      movementPhase = "release";
+    } else if (narrationHasReachedTension || fallbackTensionReached) {
+      movementPhase = "tense";
+    } else {
+      movementPhase = "idle";
+    }
+  } else if (phase === "release") {
+    // The physical release occurs on the final release cue in the preceding
+    // tension line. The dedicated release stage stays visually settled while
+    // attention moves through the released area.
+    movementPhase = "settled";
+  }
+
+  const eyesCueWordIndex = useMemo(
+    () => findPMRCue(tokens, ["eyes"]),
+    [tokens]
+  );
+
+  const foreheadCueWordIndex = useMemo(
+    () => findPMRCue(tokens, ["forehead"]),
+    [tokens]
+  );
+
+  const faceFocus = useMemo(() => {
+    if (region !== "face" || phase !== "release") return "jaw";
+
+    if (!narrate) {
+      if (phaseProgress >= 0.64) return "forehead";
+      if (phaseProgress >= 0.34) return "eyes";
+      return "jaw";
+    }
+
+    if (foreheadCueWordIndex != null && visibleCount > foreheadCueWordIndex) {
+      return "forehead";
+    }
+
+    if (eyesCueWordIndex != null && visibleCount > eyesCueWordIndex) {
+      return "eyes";
+    }
+
+    return "jaw";
+  }, [
+    region,
+    phase,
+    narrate,
+    phaseProgress,
+    foreheadCueWordIndex,
+    eyesCueWordIndex,
+    visibleCount,
+  ]);
+
+  const markerPoints =
+    region === "face" ? FACE_FOCUS_MARKERS[faceFocus] : REGION_MARKERS[region];
 
   useEffect(() => {
     const key = `${stepIndex}:${phase}:${region}`;
@@ -431,6 +532,7 @@ export default function PMRV2Stage({
         released={released}
         reducedMotion={a11y.prefs.reducedMotion}
         stepIndex={stepIndex}
+        markerPoints={markerPoints}
       />
 
       <div className="pmr-v2-copy">
diff --git a/src/index.css b/src/index.css
index e7fc0ee..7b71ee1 100644
--- a/src/index.css
+++ b/src/index.css
@@ -839,6 +839,18 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
     radial-gradient(ellipse at 80% 50%, #000 0 13%, rgba(0,0,0,0.88) 17%, rgba(0,0,0,0.18) 25%, transparent 34%);
 }
 
+.pmr-v2-motion-pose--hands-left {
+  -webkit-mask-image: radial-gradient(ellipse at 20% 50%, #000 0 13%, rgba(0,0,0,0.9) 17%, rgba(0,0,0,0.20) 25%, transparent 34%);
+  mask-image: radial-gradient(ellipse at 20% 50%, #000 0 13%, rgba(0,0,0,0.9) 17%, rgba(0,0,0,0.20) 25%, transparent 34%);
+  transform-origin: 20% 50%;
+}
+
+.pmr-v2-motion-pose--hands-right {
+  -webkit-mask-image: radial-gradient(ellipse at 80% 50%, #000 0 13%, rgba(0,0,0,0.9) 17%, rgba(0,0,0,0.20) 25%, transparent 34%);
+  mask-image: radial-gradient(ellipse at 80% 50%, #000 0 13%, rgba(0,0,0,0.9) 17%, rgba(0,0,0,0.20) 25%, transparent 34%);
+  transform-origin: 80% 50%;
+}
+
 .pmr-v2-motion-pose--shoulders {
   -webkit-mask-image: radial-gradient(ellipse at 50% 25%, rgba(0,0,0,1) 0 26%, rgba(0,0,0,0.82) 30%, rgba(0,0,0,0.16) 40%, transparent 54%);
   mask-image: radial-gradient(ellipse at 50% 25%, rgba(0,0,0,1) 0 26%, rgba(0,0,0,0.82) 30%, rgba(0,0,0,0.16) 40%, transparent 54%);
@@ -895,67 +907,75 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 }
 
 .pmr-v2-figure.movement-hands.is-body-tensing
-  .pmr-v2-motion-pose--hands {
-  animation: pmr-v2-hands-contract 1.25s var(--pmr-ease) both;
+  .pmr-v2-motion-pose--hands-left {
+  animation: pmr-v2-left-hand-clench 1.15s var(--pmr-ease) both;
 }
 
-@keyframes pmr-v2-hands-contract {
-  0% {
-    opacity: 0;
-    transform: scaleX(1.018) scaleY(1.006);
-  }
+.pmr-v2-figure.movement-hands.is-body-tensing
+  .pmr-v2-motion-pose--hands-right {
+  animation: pmr-v2-right-hand-clench 1.15s var(--pmr-ease) both;
+}
 
-  58% {
-    opacity: 0.78;
-  }
+@keyframes pmr-v2-left-hand-clench {
+  0% { opacity: 0; transform: translate3d(-1.4%, 0.8%, 0) rotate(3deg) scale(1.018); }
+  38% { opacity: 0.42; transform: translate3d(-0.7%, 0.42%, 0) rotate(1.2deg) scale(1.002); }
+  72% { opacity: 0.82; transform: translate3d(0.12%, 0.08%, 0) rotate(-1.8deg) scale(0.974); }
+  100% { opacity: 0.98; transform: translate3d(0.35%, 0, 0) rotate(-3deg) scale(0.956); }
+}
 
-  100% {
-    opacity: 0.96;
-    transform: scaleX(1) scaleY(0.996);
-  }
+@keyframes pmr-v2-right-hand-clench {
+  0% { opacity: 0; transform: translate3d(1.4%, 0.8%, 0) rotate(-3deg) scale(1.018); }
+  38% { opacity: 0.42; transform: translate3d(0.7%, 0.42%, 0) rotate(-1.2deg) scale(1.002); }
+  72% { opacity: 0.82; transform: translate3d(-0.12%, 0.08%, 0) rotate(1.8deg) scale(0.974); }
+  100% { opacity: 0.98; transform: translate3d(-0.35%, 0, 0) rotate(3deg) scale(0.956); }
+}
+
+.pmr-v2-figure.movement-hands.is-body-releasing
+  .pmr-v2-motion-pose--hands-left {
+  animation: pmr-v2-left-hand-open 1.05s var(--pmr-ease) both;
 }
 
 .pmr-v2-figure.movement-hands.is-body-releasing
-  .pmr-v2-motion-pose--hands {
-  animation: pmr-v2-hands-open 880ms var(--pmr-ease) both;
+  .pmr-v2-motion-pose--hands-right {
+  animation: pmr-v2-right-hand-open 1.05s var(--pmr-ease) both;
 }
 
-@keyframes pmr-v2-hands-open {
-  0% { opacity: 0.96; transform: scaleX(1) scaleY(0.996); }
-  48% { opacity: 0.58; }
-  100% { opacity: 0; transform: scaleX(1.018) scaleY(1.006); }
+@keyframes pmr-v2-left-hand-open {
+  0% { opacity: 0.98; transform: translate3d(0.35%, 0, 0) rotate(-3deg) scale(0.956); }
+  48% { opacity: 0.68; transform: translate3d(0, 0.18%, 0) rotate(-1deg) scale(0.982); }
+  100% { opacity: 0; transform: translate3d(-1.4%, 0.8%, 0) rotate(3deg) scale(1.018); }
+}
+
+@keyframes pmr-v2-right-hand-open {
+  0% { opacity: 0.98; transform: translate3d(-0.35%, 0, 0) rotate(3deg) scale(0.956); }
+  48% { opacity: 0.68; transform: translate3d(0, 0.18%, 0) rotate(1deg) scale(0.982); }
+  100% { opacity: 0; transform: translate3d(1.4%, 0.8%, 0) rotate(-3deg) scale(1.018); }
 }
 
 .pmr-v2-figure.movement-shoulders.is-body-tensing
   .pmr-v2-motion-pose--shoulders {
-  animation: pmr-v2-shoulders-lift 1.35s var(--pmr-ease) both;
+  animation: pmr-v2-shoulders-lift 1.5s var(--pmr-ease) both;
 }
 
 @keyframes pmr-v2-shoulders-lift {
-  0% {
-    opacity: 0;
-    transform: translateY(1.25%) scale(1.001);
-  }
-
-  52% {
-    opacity: 0.72;
-  }
-
-  100% {
-    opacity: 0.96;
-    transform: translateY(0) scale(1);
-  }
+  0% { opacity: 0; transform: translateY(1.8%) scale(1.002); }
+  28% { opacity: 0.28; transform: translateY(1.34%) scale(1.0015); }
+  58% { opacity: 0.66; transform: translateY(0.7%) scale(1.001); }
+  82% { opacity: 0.9; transform: translateY(0.2%) scale(1.0003); }
+  100% { opacity: 0.98; transform: translateY(0) scale(1); }
 }
 
 .pmr-v2-figure.movement-shoulders.is-body-releasing
   .pmr-v2-motion-pose--shoulders {
-  animation: pmr-v2-shoulders-drop 980ms var(--pmr-ease) both;
+  animation: pmr-v2-shoulders-drop 1.35s var(--pmr-ease) both;
 }
 
 @keyframes pmr-v2-shoulders-drop {
-  0% { opacity: 0.96; transform: translateY(0) scale(1); }
-  58% { opacity: 0.62; transform: translateY(0.72%) scale(1.001); }
-  100% { opacity: 0; transform: translateY(1.25%) scale(1.001); }
+  0% { opacity: 0.98; transform: translateY(0) scale(1); }
+  34% { opacity: 0.88; transform: translateY(0.38%) scale(1.0005); }
+  64% { opacity: 0.58; transform: translateY(0.96%) scale(1.001); }
+  84% { opacity: 0.26; transform: translateY(1.48%) scale(1.0015); }
+  100% { opacity: 0; transform: translateY(1.8%) scale(1.002); }
 }
 
 .pmr-v2-figure.movement-face.is-body-tensing
@@ -1081,8 +1101,10 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 .pmr-v2-body-light-mask {
   position: absolute;
   inset: 0;
+  z-index: 3;
   pointer-events: none;
   overflow: hidden;
+  mix-blend-mode: normal;
   -webkit-mask-repeat: no-repeat;
   mask-repeat: no-repeat;
   -webkit-mask-position: center;
@@ -1133,26 +1155,30 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 }
 
 .pmr-v2-glow.is-active.is-tense {
-  opacity: 0.88;
+  z-index: 2;
+  opacity: 1;
   background:
     radial-gradient(closest-side,
-      rgba(255, 218, 211, 0.94) 0%,
-      rgba(246, 135, 125, 0.66) 34%,
-      rgba(211, 82, 78, 0.20) 64%,
-      rgba(192, 67, 66, 0.07) 78%,
+      rgba(255, 229, 222, 1) 0%,
+      rgba(255, 177, 166, 0.92) 28%,
+      rgba(246, 111, 104, 0.68) 52%,
+      rgba(211, 70, 68, 0.28) 70%,
       transparent 91%);
+  filter: blur(8px);
   animation: pmr-v2-tense-gather 2.15s ease-in-out infinite;
 }
 
 .pmr-v2-glow.is-active.is-release {
-  opacity: 0.96;
+  z-index: 2;
+  opacity: 1;
   background:
     radial-gradient(closest-side,
-      rgba(255, 225, 218, 0.92) 0%,
-      rgba(246, 145, 134, 0.62) 34%,
-      rgba(211, 82, 78, 0.18) 64%,
-      rgba(192, 67, 66, 0.06) 78%,
+      rgba(255, 232, 226, 1) 0%,
+      rgba(255, 185, 174, 0.9) 30%,
+      rgba(246, 121, 112, 0.64) 54%,
+      rgba(211, 76, 72, 0.24) 72%,
       transparent 91%);
+  filter: blur(9px);
   animation: pmr-v2-release-open 2.6s var(--pmr-ease) both;
 }
 
@@ -1183,6 +1209,13 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
     0 0 0 7px rgba(246, 135, 125, 0.055),
     0 0 24px rgba(231, 102, 94, 0.24);
   transform: translate(-50%, -50%);
+  transition:
+    top 720ms var(--pmr-ease),
+    left 720ms var(--pmr-ease),
+    border-color 420ms ease,
+    background 420ms ease,
+    box-shadow 420ms ease;
+  will-change: top, left, transform, opacity;
   animation: pmr-v2-focus-draw 2.15s ease-in-out infinite;
 }
 
@@ -1342,7 +1375,7 @@ html.reduce-motion
   margin: 0;
   color: #edf0e3;
   font-family: var(--font-heading);
-  font-size: clamp(0.72rem, 2.18vh, 1.03rem);
+  font-size: clamp(0.9rem, 2.73vh, 1.29rem);
   font-weight: 500;
   line-height: 1.08;
   letter-spacing: -0.018em;
@@ -1458,7 +1491,8 @@ html.reduce-motion
 
   .pmr-v2-figure,
   .pmr-v2-body-wrap,
-  .pmr-v2-glow { transition-duration: 0ms !important; }
+  .pmr-v2-glow,
+  .pmr-v2-tension-focus { transition-duration: 0ms !important; }
 }
 
 html.reduce-motion .pmr-v2-figure-halo,
@@ -1470,7 +1504,8 @@ html.reduce-motion .pmr-v2-whole-sweep { animation: none !important; }
 
 html.reduce-motion .pmr-v2-figure,
 html.reduce-motion .pmr-v2-body-wrap,
-html.reduce-motion .pmr-v2-glow { transition-duration: 0ms !important; }
+html.reduce-motion .pmr-v2-glow,
+html.reduce-motion .pmr-v2-tension-focus { transition-duration: 0ms !important; }
 
 /* =========================================================
    PMR V2 — refined spacing + next body part control
