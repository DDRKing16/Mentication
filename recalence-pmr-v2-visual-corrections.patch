diff --git a/src/components/PMRV2Stage.jsx b/src/components/PMRV2Stage.jsx
index fd1c221..9f87e22 100644
--- a/src/components/PMRV2Stage.jsx
+++ b/src/components/PMRV2Stage.jsx
@@ -15,16 +15,6 @@ const PMR_POSE_ASSETS = {
   lowerLegs: "/media/images/pmr-motion/pmr-lower-legs-tense.png",
 };
 
-const PMR_MOVEMENT_COPY = {
-  hands: "Clench the hands",
-  shoulders: "Lift the shoulders",
-  face: "Tighten the face",
-  torso: "Brace the centre",
-  hips: "Squeeze through the hips",
-  thighs: "Tense the thighs",
-  lowerLegs: "Press through the feet",
-};
-
 function normalisePMRWord(text = "") {
   return String(text)
     .toLowerCase()
@@ -90,12 +80,12 @@ const SECTION_FOR_REGION = {
 // The light is clipped to a real-alpha PNG, so no rectangular glow leaks
 // outside the figure even when the radial fields overlap.
 const REGION_BOX = {
-  face: { top: "2%", left: "37%", width: "26%", height: "15%" },
+  face: { top: "11%", left: "40%", width: "20%", height: "8%" },
   shoulders: [
     { top: "14%", left: "15%", width: "34%", height: "24%" },
     { top: "14%", left: "51%", width: "34%", height: "24%" },
   ],
-  torso: { top: "20%", left: "29%", width: "42%", height: "28%" },
+  torso: { top: "30%", left: "32%", width: "36%", height: "20%" },
   hands: [
     { top: "35%", left: "3%", width: "26%", height: "23%" },
     { top: "35%", left: "71%", width: "26%", height: "23%" },
@@ -112,15 +102,30 @@ const REGION_BOX = {
   whole: { top: "1%", left: "7%", width: "86%", height: "98%" },
 };
 
-const REGION_CENTER = {
-  hands: { top: "47%", left: "50%" },
-  shoulders: { top: "23%", left: "50%" },
-  face: { top: "10%", left: "50%" },
-  torso: { top: "33%", left: "50%" },
-  hips: { top: "50%", left: "50%" },
-  thighs: { top: "64%", left: "50%" },
-  lowerLegs: { top: "84%", left: "50%" },
-  whole: { top: "50%", left: "50%" },
+const REGION_MARKERS = {
+  hands: [
+    { top: "50%", left: "20%" },
+    { top: "50%", left: "80%" },
+  ],
+  shoulders: [
+    { top: "21%", left: "36%" },
+    { top: "21%", left: "64%" },
+  ],
+  face: [{ top: "15.5%", left: "50%" }],
+  torso: [{ top: "38%", left: "50%" }],
+  hips: [
+    { top: "51%", left: "42%" },
+    { top: "51%", left: "58%" },
+  ],
+  thighs: [
+    { top: "62%", left: "41%" },
+    { top: "62%", left: "59%" },
+  ],
+  lowerLegs: [
+    { top: "80%", left: "42%" },
+    { top: "80%", left: "58%" },
+  ],
+  whole: [{ top: "50%", left: "50%" }],
 };
 
 const PHASE_COPY = {
@@ -169,6 +174,38 @@ function GlowBoxes({ name, active, phase, settled }) {
   ));
 }
 
+function MarkerPoints({ region, phase, stepIndex, reducedMotion }) {
+  const points = REGION_MARKERS[region] || [];
+
+  return points.map((point, index) => (
+    <div
+      key={`focus-${region}-${stepIndex}-${index}`}
+      className={[
+        "pmr-v2-tension-focus",
+        `is-${phase}`,
+        reducedMotion ? "is-reduced-motion" : "",
+      ]
+        .filter(Boolean)
+        .join(" ")}
+      style={point}
+      aria-hidden="true"
+    />
+  ));
+}
+
+function RipplePoints({ region, stepIndex }) {
+  const points = REGION_MARKERS[region] || [];
+
+  return points.map((point, index) => (
+    <div
+      key={`ripple-${region}-${stepIndex}-${index}`}
+      className="pmr-v2-ripple"
+      style={{ ...point, animationDelay: `${index * 90}ms` }}
+      aria-hidden="true"
+    />
+  ));
+}
+
 function BodyFigure({
   region,
   phase,
@@ -199,11 +236,6 @@ function BodyFigure({
     region !== "whole" &&
     !reducedMotion;
 
-  const currentPoseAsset =
-    activeRegion && PMR_POSE_ASSETS[activeRegion]
-      ? PMR_POSE_ASSETS[activeRegion]
-      : null;
-
   const maskAsset = BODY_ASSET;
 
   return (
@@ -287,23 +319,18 @@ function BodyFigure({
           )}
 
           {showReleaseRipple && (
-            <div
-              key={`ripple-${region}-${stepIndex}`}
-              className="pmr-v2-ripple"
-              style={REGION_CENTER[region]}
-            />
+            <RipplePoints region={region} stepIndex={stepIndex} />
           )}
         </div>
 
-        {movementPhase === "tense" &&
-          activeRegion &&
-          !reducedMotion && (
-            <div
-              key={`focus-${region}-${stepIndex}`}
-              className="pmr-v2-tension-focus"
-              style={REGION_CENTER[region]}
-            />
-          )}
+        {activeRegion && (
+          <MarkerPoints
+            region={activeRegion}
+            phase={movementPhase}
+            stepIndex={stepIndex}
+            reducedMotion={reducedMotion}
+          />
+        )}
       </div>
     </div>
   );
@@ -340,6 +367,7 @@ export default function PMRV2Stage({
   elapsed = 0,
   onNarrationEnd,
   onNextBodyPart,
+  nextBodyPartLabel = "Next body part",
 }) {
   const a11y = useAccessibilityPrefs();
   const previousStep = useRef(null);
@@ -416,9 +444,9 @@ export default function PMRV2Stage({
               type="button"
               className="pmr-v2-next-body"
               onClick={onNextBodyPart}
-              aria-label="Move to next body part"
+              aria-label={nextBodyPartLabel}
             >
-              <span>Next body part</span>
+              <span>{nextBodyPartLabel}</span>
               <span className="pmr-v2-next-body-arrow" aria-hidden="true">
                 →
               </span>
diff --git a/src/components/ResetPlayer.jsx b/src/components/ResetPlayer.jsx
index 59803e1..a476326 100644
--- a/src/components/ResetPlayer.jsx
+++ b/src/components/ResetPlayer.jsx
@@ -301,34 +301,33 @@ export default function ResetPlayer({ pathway, answers, onComplete, onExit }) {
   };
 
   // ---- PMR V2: move directly to the next muscle group ----
-  const nextPMRBodyPartIndex = useMemo(() => {
+  // Individual muscle phases skip the matching release phase and land on the
+  // next region. Intro and whole-body closing phases advance one stage at a
+  // time so this control remains available throughout the full experience.
+  const nextPMRStageIndex = useMemo(() => {
     if (!isPMRV2 || !step || !Array.isArray(iv?.steps)) return -1;
 
     const currentRegion = step.region;
 
-    // No body-part skipping during intro / whole-body closing stages.
-    if (!currentRegion || currentRegion === "whole") return -1;
+    if (stepIndex >= iv.steps.length - 1) return iv.steps.length;
+
+    if (!currentRegion || currentRegion === "whole") {
+      return stepIndex + 1;
+    }
 
     for (let i = stepIndex + 1; i < iv.steps.length; i += 1) {
       const candidate = iv.steps[i];
 
-      // Find the next individual muscle group.
-      // Skip any second phase belonging to the current region,
-      // and do not treat the whole-body closing stages as a body part.
-      if (
-        candidate?.region &&
-        candidate.region !== currentRegion &&
-        candidate.region !== "whole"
-      ) {
+      if (candidate?.region && candidate.region !== currentRegion) {
         return i;
       }
     }
 
-    return -1;
+    return iv.steps.length;
   }, [isPMRV2, step, stepIndex, iv?.steps]);
 
   const nextPMRBodyPart = useCallback(() => {
-    if (nextPMRBodyPartIndex < 0) return;
+    if (nextPMRStageIndex < 0) return;
 
     if (transitionTimer.current) {
       clearTimeout(transitionTimer.current);
@@ -339,19 +338,27 @@ export default function ResetPlayer({ pathway, answers, onComplete, onExit }) {
 
     setTransition(null);
 
+    if (nextPMRStageIndex >= iv.steps.length) {
+      setNarrationEnded(true);
+      goNextStep();
+      return;
+    }
+
     // Count skipped PMR stages as completed for overall progress.
     setStepsDone((done) =>
-      done + Math.max(1, nextPMRBodyPartIndex - stepIndex)
+      done + Math.max(1, nextPMRStageIndex - stepIndex)
     );
 
-    setStepIndex(nextPMRBodyPartIndex);
+    setStepIndex(nextPMRStageIndex);
     setElapsed(0);
     setNarrationEnded(false);
     setRunning(true);
   }, [
-    nextPMRBodyPartIndex,
+    nextPMRStageIndex,
     stepIndex,
     stopVoice,
+    iv?.steps?.length,
+    goNextStep,
   ]);
 
   // ---- "this isn't helping" switch ----
@@ -574,9 +581,8 @@ export default function ResetPlayer({ pathway, answers, onComplete, onExit }) {
                   leadMs={ivIndex === 0 && stepIndex === 0 ? vf.leadMs : 250}
                   spoken={spokenFor(step, iv, answers?.direction)}
                   onNarrationEnd={() => setNarrationEnded(true)}
-                  onNextBodyPart={
-                    nextPMRBodyPartIndex >= 0 ? nextPMRBodyPart : undefined
-                  }
+                  onNextBodyPart={nextPMRStageIndex >= 0 ? nextPMRBodyPart : undefined}
+                  nextBodyPartLabel={step?.phase === "return" ? "Finish" : "Next body part"}
                 />
               ) : (
                 <StageVisual
@@ -803,4 +809,4 @@ function CtrlButton({ children, onClick, active, label, light = false }) {
       {children}
     </button>
   );
-}
\ No newline at end of file
+}
diff --git a/src/index.css b/src/index.css
index 016d981..e7fc0ee 100644
--- a/src/index.css
+++ b/src/index.css
@@ -689,13 +689,13 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 }
 
 .pmr-v2-figure.is-tense {
-  transform: scale(0.992);
+  transform: none;
   filter: saturate(1.05) contrast(1.035);
 }
 
 .pmr-v2-figure.is-release,
 .pmr-v2-figure.is-rest {
-  transform: scale(1.006);
+  transform: none;
   filter: saturate(0.98) brightness(1.025);
 }
 
@@ -743,15 +743,19 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 }
 
 .pmr-v2-body-wrap {
-  position: relative;
-  width: 100%;
+  position: absolute;
+  top: 0;
+  left: 50%;
+  width: auto;
   height: 100%;
+  aspect-ratio: 941 / 1672;
+  transform: translateX(-50%);
   transition: transform 900ms var(--pmr-ease);
 }
 
-.pmr-v2-figure.is-tense .pmr-v2-body-wrap { transform: scale(0.994); }
+.pmr-v2-figure.is-tense .pmr-v2-body-wrap { transform: translateX(-50%); }
 .pmr-v2-figure.is-release .pmr-v2-body-wrap,
-.pmr-v2-figure.is-rest .pmr-v2-body-wrap { transform: scale(1.004); }
+.pmr-v2-figure.is-rest .pmr-v2-body-wrap { transform: translateX(-50%); }
 
 .pmr-v2-body-img {
   display: block;
@@ -827,8 +831,12 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 }
 
 .pmr-v2-motion-pose--hands {
-  -webkit-mask-image: radial-gradient(ellipse at 39% 48%, rgba(0,0,0,1) 0 20%, rgba(0,0,0,0.88) 23%, rgba(0,0,0,0.14) 31%, transparent 41%);
-  mask-image: radial-gradient(ellipse at 39% 48%, rgba(0,0,0,1) 0 20%, rgba(0,0,0,0.88) 23%, rgba(0,0,0,0.14) 31%, transparent 41%);
+  -webkit-mask-image:
+    radial-gradient(ellipse at 20% 50%, #000 0 13%, rgba(0,0,0,0.88) 17%, rgba(0,0,0,0.18) 25%, transparent 34%),
+    radial-gradient(ellipse at 80% 50%, #000 0 13%, rgba(0,0,0,0.88) 17%, rgba(0,0,0,0.18) 25%, transparent 34%);
+  mask-image:
+    radial-gradient(ellipse at 20% 50%, #000 0 13%, rgba(0,0,0,0.88) 17%, rgba(0,0,0,0.18) 25%, transparent 34%),
+    radial-gradient(ellipse at 80% 50%, #000 0 13%, rgba(0,0,0,0.88) 17%, rgba(0,0,0,0.18) 25%, transparent 34%);
 }
 
 .pmr-v2-motion-pose--shoulders {
@@ -837,8 +845,8 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 }
 
 .pmr-v2-motion-pose--face {
-  -webkit-mask-image: radial-gradient(ellipse at 50% 18%, rgba(0,0,0,1) 0 18%, rgba(0,0,0,0.82) 25%, rgba(0,0,0,0.18) 33%, transparent 44%);
-  mask-image: radial-gradient(ellipse at 50% 18%, rgba(0,0,0,1) 0 18%, rgba(0,0,0,0.82) 25%, rgba(0,0,0,0.18) 33%, transparent 44%);
+  -webkit-mask-image: radial-gradient(ellipse at 50% 15.5%, rgba(0,0,0,1) 0 8%, rgba(0,0,0,0.82) 12%, rgba(0,0,0,0.18) 18%, transparent 27%);
+  mask-image: radial-gradient(ellipse at 50% 15.5%, rgba(0,0,0,1) 0 8%, rgba(0,0,0,0.82) 12%, rgba(0,0,0,0.18) 18%, transparent 27%);
 }
 
 .pmr-v2-motion-pose--torso {
@@ -869,22 +877,21 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 }
 
 .pmr-v2-motion-pose.is-current.is-release {
-  opacity: 0;
-  transform:
-    translate3d(0, 0, 0)
-    scale(1.002);
-
-  transition:
-    opacity 1100ms var(--pmr-ease),
-    transform 1200ms var(--pmr-ease);
+  animation: pmr-v2-local-release 900ms var(--pmr-ease) both;
 }
 
 .pmr-v2-figure.is-body-tensing .pmr-v2-body-wrap {
-  transform: scale(0.997);
+  transform: translateX(-50%);
 }
 
 .pmr-v2-figure.is-body-releasing .pmr-v2-body-wrap {
-  transform: scale(1.004);
+  transform: translateX(-50%);
+}
+
+@keyframes pmr-v2-local-release {
+  0% { opacity: 0.96; transform: translate3d(0, 0, 0) scale(1); }
+  55% { opacity: 0.42; }
+  100% { opacity: 0; transform: translate3d(0, 0.2%, 0) scale(1.001); }
 }
 
 .pmr-v2-figure.movement-hands.is-body-tensing
@@ -895,15 +902,30 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 @keyframes pmr-v2-hands-contract {
   0% {
     opacity: 0;
-    transform: scale(1.002);
+    transform: scaleX(1.018) scaleY(1.006);
+  }
+
+  58% {
+    opacity: 0.78;
   }
 
   100% {
     opacity: 0.96;
-    transform: scale(1);
+    transform: scaleX(1) scaleY(0.996);
   }
 }
 
+.pmr-v2-figure.movement-hands.is-body-releasing
+  .pmr-v2-motion-pose--hands {
+  animation: pmr-v2-hands-open 880ms var(--pmr-ease) both;
+}
+
+@keyframes pmr-v2-hands-open {
+  0% { opacity: 0.96; transform: scaleX(1) scaleY(0.996); }
+  48% { opacity: 0.58; }
+  100% { opacity: 0; transform: scaleX(1.018) scaleY(1.006); }
+}
+
 .pmr-v2-figure.movement-shoulders.is-body-tensing
   .pmr-v2-motion-pose--shoulders {
   animation: pmr-v2-shoulders-lift 1.35s var(--pmr-ease) both;
@@ -912,7 +934,11 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 @keyframes pmr-v2-shoulders-lift {
   0% {
     opacity: 0;
-    transform: translateY(0.35%) scale(1.001);
+    transform: translateY(1.25%) scale(1.001);
+  }
+
+  52% {
+    opacity: 0.72;
   }
 
   100% {
@@ -921,6 +947,17 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
   }
 }
 
+.pmr-v2-figure.movement-shoulders.is-body-releasing
+  .pmr-v2-motion-pose--shoulders {
+  animation: pmr-v2-shoulders-drop 980ms var(--pmr-ease) both;
+}
+
+@keyframes pmr-v2-shoulders-drop {
+  0% { opacity: 0.96; transform: translateY(0) scale(1); }
+  58% { opacity: 0.62; transform: translateY(0.72%) scale(1.001); }
+  100% { opacity: 0; transform: translateY(1.25%) scale(1.001); }
+}
+
 .pmr-v2-figure.movement-face.is-body-tensing
   .pmr-v2-motion-pose--face {
   animation: pmr-v2-face-contract 1.05s var(--pmr-ease) both;
@@ -929,15 +966,30 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 @keyframes pmr-v2-face-contract {
   0% {
     opacity: 0;
-    transform: scale(1.001);
+    transform: scaleX(1) scaleY(1);
+  }
+
+  55% {
+    opacity: 0.72;
   }
 
   100% {
     opacity: 0.96;
-    transform: scale(1);
+    transform: scaleX(1.006) scaleY(0.992);
   }
 }
 
+.pmr-v2-figure.movement-face.is-body-releasing
+  .pmr-v2-motion-pose--face {
+  animation: pmr-v2-jaw-soften 850ms var(--pmr-ease) both;
+}
+
+@keyframes pmr-v2-jaw-soften {
+  0% { opacity: 0.96; transform: scaleX(1.006) scaleY(0.992); }
+  52% { opacity: 0.56; }
+  100% { opacity: 0; transform: scaleX(1) scaleY(1); }
+}
+
 .pmr-v2-figure.movement-torso.is-body-tensing
   .pmr-v2-motion-pose--torso {
   animation: pmr-v2-torso-brace 1.35s var(--pmr-ease) both;
@@ -1023,7 +1075,7 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 
 .pmr-v2-figure.is-body-releasing .pmr-v2-body-img--neutral {
   opacity: 0.94;
-  transform: translateY(0.08%) scale(1.001);
+  transform: none;
 }
 
 .pmr-v2-body-light-mask {
@@ -1082,14 +1134,32 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
 
 .pmr-v2-glow.is-active.is-tense {
   opacity: 0.88;
+  background:
+    radial-gradient(closest-side,
+      rgba(255, 218, 211, 0.94) 0%,
+      rgba(246, 135, 125, 0.66) 34%,
+      rgba(211, 82, 78, 0.20) 64%,
+      rgba(192, 67, 66, 0.07) 78%,
+      transparent 91%);
   animation: pmr-v2-tense-gather 2.15s ease-in-out infinite;
 }
 
 .pmr-v2-glow.is-active.is-release {
   opacity: 0.96;
+  background:
+    radial-gradient(closest-side,
+      rgba(255, 225, 218, 0.92) 0%,
+      rgba(246, 145, 134, 0.62) 34%,
+      rgba(211, 82, 78, 0.18) 64%,
+      rgba(192, 67, 66, 0.06) 78%,
+      transparent 91%);
   animation: pmr-v2-release-open 2.6s var(--pmr-ease) both;
 }
 
+.pmr-v2-glow.is-active .pmr-v2-glow-core {
+  background: radial-gradient(circle, rgba(255, 232, 226, 0.88), rgba(246, 135, 125, 0.20) 66%, transparent 78%);
+}
+
 @keyframes pmr-v2-tense-gather {
   0%, 100% { opacity: 0.66; transform: scale(0.98); filter: blur(12px); }
   50% { opacity: 0.98; transform: scale(0.80); filter: blur(8px); }
@@ -1107,14 +1177,30 @@ html.reduce-motion .grounding-orb-glow { animation: none !important; opacity: 0.
   width: 0.7rem;
   height: 0.7rem;
   border-radius: 999px;
-  border: 1px solid rgba(218, 247, 216, 0.68);
+  border: 1px solid rgba(255, 221, 215, 0.76);
+  background: rgba(246, 135, 125, 0.18);
   box-shadow:
-    0 0 0 7px rgba(112, 213, 157, 0.045),
-    0 0 24px rgba(116, 224, 164, 0.20);
+    0 0 0 7px rgba(246, 135, 125, 0.055),
+    0 0 24px rgba(231, 102, 94, 0.24);
   transform: translate(-50%, -50%);
   animation: pmr-v2-focus-draw 2.15s ease-in-out infinite;
 }
 
+.pmr-v2-tension-focus.is-release {
+  opacity: 0.54;
+  animation: pmr-v2-focus-release 1050ms var(--pmr-ease) both;
+}
+
+.pmr-v2-tension-focus.is-rest,
+.pmr-v2-tension-focus.is-return {
+  display: none;
+}
+
+@keyframes pmr-v2-focus-release {
+  0% { opacity: 0.62; transform: translate(-50%, -50%) scale(0.78); }
+  100% { opacity: 0.18; transform: translate(-50%, -50%) scale(1.55); }
+}
+
 @media (prefers-reduced-motion: reduce) {
   .pmr-v2-motion-pose,
   .pmr-v2-body-img--neutral {
@@ -1159,8 +1245,8 @@ html.reduce-motion
   width: 0.4rem;
   height: 0.4rem;
   border-radius: 50%;
-  border: 1px solid rgba(224, 248, 218, 0.66);
-  box-shadow: 0 0 12px rgba(151, 229, 177, 0.20);
+  border: 1px solid rgba(255, 218, 211, 0.70);
+  box-shadow: 0 0 12px rgba(231, 102, 94, 0.22);
   transform: translate(-50%, -50%) scale(1);
   opacity: 0;
   animation: pmr-v2-ripple-out 2.35s var(--pmr-ease) both;
@@ -1175,7 +1261,7 @@ html.reduce-motion
   position: absolute;
   border-radius: 42%;
   background:
-    radial-gradient(ellipse at 50% 40%, rgba(229, 246, 214, 0.62), rgba(115, 213, 157, 0.30) 52%, rgba(221, 187, 116, 0.16) 74%, transparent 92%);
+    radial-gradient(ellipse at 50% 40%, rgba(255, 224, 216, 0.58), rgba(244, 143, 132, 0.28) 52%, rgba(213, 91, 84, 0.13) 74%, transparent 92%);
   filter: blur(14px);
   opacity: 0.68;
   animation: pmr-v2-whole-breathe 6.4s ease-in-out infinite;
@@ -1256,7 +1342,7 @@ html.reduce-motion
   margin: 0;
   color: #edf0e3;
   font-family: var(--font-heading);
-  font-size: clamp(1.42rem, 4.35vh, 2.05rem);
+  font-size: clamp(0.72rem, 2.18vh, 1.03rem);
   font-weight: 500;
   line-height: 1.08;
   letter-spacing: -0.018em;
@@ -1707,4 +1793,4 @@ html.reduce-motion .grounding-taste-glow--settled { animation: none !important;
    so surrounding stage markers / controls never jump. */
 .grounding-instruction-sentence {
   min-height: calc(1.7em * 2);
-}
\ No newline at end of file
+}
diff --git a/src/lib/interventions.js b/src/lib/interventions.js
index 2199301..9aa58cf 100644
--- a/src/lib/interventions.js
+++ b/src/lib/interventions.js
@@ -588,7 +588,7 @@ const SOMATIC = [
   why: "A guided contrast between gentle effort and longer release helps the body recognise and settle muscular tension.",
   targets: ["body"],
   states: ["tense", "cant_sleep", "restless", "overwhelmed"],
-  durationMin: 7,
+  durationMin: 4,
   bedtime: true,
   physicalDemand: 2,
   movement: "seated",
@@ -602,16 +602,16 @@ const SOMATIC = [
       speak: "Back supported, hands resting in your lap.",
       region: "whole",
       phase: "intro",
-      holdSec: 24,
+      holdSec: 14,
     },
 
     {
-      title: "Hands and forearms",
+      title: "Squeeze both hands",
       body: "Gently tense your hands and forearms. Hold. Three, two, one. And let go.",
       speak: "Gently tense your hands and forearms. Hold. Three, two, one. And let go.",
       region: "hands",
       phase: "tense",
-      holdSec: 15,
+      holdSec: 9,
     },
     {
       title: "Let the hands soften",
@@ -619,16 +619,16 @@ const SOMATIC = [
       speak: "Let your hands and forearms soften completely. Notice the warmth and heaviness.",
       region: "hands",
       phase: "release",
-      holdSec: 24,
+      holdSec: 14,
     },
 
     {
-      title: "Arms and shoulders",
+      title: "Lift your shoulders",
       body: "Now tense your arms and shoulders. Lift slightly. Hold. Three, two, one. And release.",
       speak: "Now tense your arms and shoulders. Lift slightly. Hold. Three, two, one. And release.",
       region: "shoulders",
       phase: "tense",
-      holdSec: 15,
+      holdSec: 9,
     },
     {
       title: "Let the shoulders drop",
@@ -636,50 +636,50 @@ const SOMATIC = [
       speak: "Let your arms and shoulders drop and soften. Feel that area loosen.",
       region: "shoulders",
       phase: "release",
-      holdSec: 24,
+      holdSec: 14,
     },
 
     {
-      title: "Jaw and face",
+      title: "Gently tense your jaw",
       body: "Gently tense your jaw and face. Hold. Three, two, one. And let everything soften.",
       speak: "Gently tense your jaw and face. Hold. Three, two, one. And let everything soften.",
       region: "face",
       phase: "tense",
-      holdSec: 15,
+      holdSec: 9,
     },
     {
-      title: "Soften the face",
+      title: "Let your jaw soften",
       body: "Let your jaw, your eyes, and your forehead soften completely.",
       speak: "Let your jaw, your eyes, and your forehead soften completely.",
       region: "face",
       phase: "release",
-      holdSec: 24,
+      holdSec: 14,
     },
 
     {
-      title: "Chest and stomach",
+      title: "Gently tighten your stomach",
       body: "Gently tighten your chest and stomach. Hold. Three, two, one. And release.",
       speak: "Gently tighten your chest and stomach. Hold. Three, two, one. And release.",
       region: "torso",
       phase: "tense",
-      holdSec: 15,
+      holdSec: 9,
     },
     {
-      title: "Soften through the centre",
+      title: "Let your stomach soften",
       body: "Let your chest and stomach soften. Allow the centre of your body to relax.",
       speak: "Let your chest and stomach soften. Allow the centre of your body to relax.",
       region: "torso",
       phase: "release",
-      holdSec: 24,
+      holdSec: 14,
     },
 
     {
-      title: "Hips and glutes",
+      title: "Gently tense your hips",
       body: "Tense your hips and glutes. Hold. Three, two, one. And let go.",
       speak: "Tense your hips and glutes. Hold. Three, two, one. And let go.",
       region: "hips",
       phase: "tense",
-      holdSec: 15,
+      holdSec: 9,
     },
     {
       title: "Let the hips soften",
@@ -687,16 +687,16 @@ const SOMATIC = [
       speak: "Let your hips and glutes soften completely. Feel the weight settling down.",
       region: "hips",
       phase: "release",
-      holdSec: 24,
+      holdSec: 14,
     },
 
     {
-      title: "Thighs",
+      title: "Gently tense your thighs",
       body: "Gently tense your thighs. Hold. Three, two, one. And release.",
       speak: "Gently tense your thighs. Hold. Three, two, one. And release.",
       region: "thighs",
       phase: "tense",
-      holdSec: 15,
+      holdSec: 9,
     },
     {
       title: "Let the thighs grow heavy",
@@ -704,16 +704,16 @@ const SOMATIC = [
       speak: "Let your thighs soften and grow heavy. Notice the release in your legs.",
       region: "thighs",
       phase: "release",
-      holdSec: 24,
+      holdSec: 14,
     },
 
     {
-      title: "Calves and feet",
+      title: "Press through your feet",
       body: "Tense your calves and feet. Hold. Three, two, one. And let go completely.",
       speak: "Tense your calves and feet. Hold. Three, two, one. And let go completely.",
       region: "lowerLegs",
       phase: "tense",
-      holdSec: 15,
+      holdSec: 9,
     },
     {
       title: "Let the lower legs settle",
@@ -721,16 +721,16 @@ const SOMATIC = [
       speak: "Let your calves and feet soften. Let them feel warm, heavy, and at rest.",
       region: "lowerLegs",
       phase: "release",
-      holdSec: 24,
+      holdSec: 14,
     },
 
     {
-      title: "Notice the whole body",
+      title: "Let everything soften",
       body: "Notice your whole body now. Let every remaining bit of tension soften.",
       speak: "Notice your whole body now. Let every remaining bit of tension soften.",
       region: "whole",
       phase: "release",
-      holdSec: 40,
+      holdSec: 24,
     },
     {
       title: "Nothing else to do",
@@ -738,7 +738,7 @@ const SOMATIC = [
       speak: "Let your whole body rest. Nothing else to do right now.",
       region: "whole",
       phase: "rest",
-      holdSec: 30,
+      holdSec: 18,
     },
     {
       title: "Come back gradually",
@@ -746,7 +746,7 @@ const SOMATIC = [
       speak: "Gently widen your focus to the whole room. Feel your feet, your seat, and one steady breath. You are here, now.",
       region: "whole",
       phase: "return",
-      holdSec: 24,
+      holdSec: 14,
     },
   ],
 }),
@@ -1995,4 +1995,4 @@ export function transitionSentence(nextIv) {
     "Now, we’ll move gently forward.",
   ];
   return templates[Math.floor(Math.random() * templates.length)];
-}
\ No newline at end of file
+}
